###  Experimental environment
The environment.yml file is generated with the terminal command `conda env export > environment.yml`.You can run the following command in the terminal to recreate the experimental environment: `conda env create -f environment.yml`.

###  